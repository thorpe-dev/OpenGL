#ifndef _PARSER_H
#define _PARSER_H

#include <string>
#include <vector>
#include <glm/glm.hpp>

using namespace std;

struct ppm_file
{
    int width;
    int height;
    int max;
    int bytecount;
    char* texture;
};

void parse_vtk_file(char* s);
ppm_file* parse_ppm_file(char* s);
string read_file(char* fname);

#endif